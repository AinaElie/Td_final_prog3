spring.application.name=Restaurant_Central

spring.datasource.url=jdbc:postgresql://localhost:5432/restaurant_management_spring_boot_center
spring.datasource.username=postgres
spring.datasource.password=raharison#77
spring.datasource.driver-class-name=org.postgresql.Driver
spring.sql.init.mode=never

# Désactive la configuration automatique des données JDBC
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,org.springframework.boot.autoconfigure.data.jdbc.JdbcRepositoriesAutoConfiguration

# Autre configuration
server.port=8081
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# Dans application.properties
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.springframework.transaction=DEBUG

# Pour activer le rechargement Ã  chaud de Spring DevTools
spring.devtools.restart.enabled=true

api.pos1.url=http://localhost:8080
api.pos2.url=http://localhost:8082